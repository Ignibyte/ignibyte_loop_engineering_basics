{
  "name": "ignibyte-loop-engineering-basics-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Browser regression tests for the notes app. The Rust service itself needs no npm.",
  "scripts": {
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
